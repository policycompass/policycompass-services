"""WSGI config for policycompass development setup

It exposes the WSGI callable as a module-level variable named ``application`` running with
development config.

"""

from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()
